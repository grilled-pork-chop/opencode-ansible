# Python / uv
.venv/
__pycache__/
*.pyc
.pytest_cache/

# Ansible
*.retry
.ansible/

# Molecule ephemeral state
tests/molecule/*/.molecule/
tests/molecule/*/tmp/

# Editor / local agent state
.DS_Store
*.swp
*.swo
.claude/

# binary + plugin runtime — fetched via `make fetch`, too large for git
roles/opencode/files/opencode
roles/opencode/files/opencode-dependencies.tar.gz

# scratch install tree written by `make local`
tmp/
